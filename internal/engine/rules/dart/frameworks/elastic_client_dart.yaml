frameworks:
  name: elastic_client
  pub_package: elastic_client
  detection:
    pubspec_markers:
    - elastic_client
    import_patterns:
    - package:elastic_client/
    code_patterns:
    - ElasticClient(
    - client.search(
  notes: |
    elastic_client is the primary Dart Elasticsearch client (pub.dev).
