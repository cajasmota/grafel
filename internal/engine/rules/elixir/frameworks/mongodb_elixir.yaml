frameworks:
  name: mongodb_driver (Elixir)
  hex_package: mongodb_driver
  category: nosql_driver
  detection:
    mix_deps:
      - mongodb_driver
      - mongodb_ecto
    import_markers:
      - Mongo
    code_patterns:
      - Mongo.start_link(
      - Mongo.find(
      - Mongo.insert_one(
      - Mongo.aggregate(

