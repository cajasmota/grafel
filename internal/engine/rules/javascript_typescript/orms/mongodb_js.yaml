orms_and_database:
  name: mongodb (official Node.js driver)
  npm_packages:
    - mongodb
  config_files: []
  detection_signals:
    package_json_deps:
      - mongodb
    import_patterns:
      - from 'mongodb'
      - require('mongodb')
    code_patterns:
      - new MongoClient(
      - MongoClient.connect(
      - client.db(
      - collection.aggregate(
      - collection.watch(

