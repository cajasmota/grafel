database_libraries:
  name: go-redis
  module_path: github.com/redis/go-redis/v9
  category: cache_client
  github_stars_2025: 20600+
  detection:
    go_mod_require:
    - github.com/redis/go-redis/v9
    import_markers:
    - '"github.com/redis/go-redis/v9"'
    structural_markers:
    - redis.NewClient(
    - redis.NewClusterClient(
    - rdb.Set(
    - rdb.Get(
  notes: 'Official Redis Go client. v9 is current (requires /v9 suffix). Cluster and
    sentinel configurations are additional structural signals for distributed Redis
    setups.

    '

