frameworks:
  name: MongoSwift / MongoKitten
  spm_package: https://github.com/mongodb/mongo-swift-driver
  category: nosql_driver
  detection:
    package_swift_deps:
      - MongoSwift
      - MongoKitten
    import_markers:
      - import MongoSwift
      - import MongoKitten
    code_patterns:
      - MongoClient(
      - MongoDatabase(
      - collection.aggregate(

