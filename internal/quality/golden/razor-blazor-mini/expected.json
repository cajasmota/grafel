{
  "fixture_name": "razor-blazor-mini",
  "language": "razor",
  "description": "Minimal Blazor component tree: a base component, a layout that inherits it, a component carrying two @implements directives whose interfaces are declared in a C# file beside it, and a component with no hierarchy directive at all. Exercises component extraction, component->event-handler CONTAINS, and — the reason the fixture exists (#6370) — the @inherits and @implements directives, including the three places razor text LOOKS like a directive and must NOT produce an edge: mid-line markup, a one-line @* *@ comment, and a multi-line one.",
  "expected_entities": [
    { "name": "AppLayoutBase", "kind": "SCOPE.UIComponent", "subtype": "component", "source_file": "Components/Layout/AppLayoutBase.razor", "must_exist": true,
      "note": "The base MainLayout inherits. It declares no directive of its own — a base component is also a plain component. The subtype is load-bearing here and on every component row below: SCOPE.UIComponent is ALSO the kind razor gives an @inject entity (`Js` in Counter.razor), so a kind-only row is satisfied by a record that is not a component at all." },
    { "name": "MainLayout", "kind": "SCOPE.UIComponent", "subtype": "component", "source_file": "Components/Layout/MainLayout.razor", "must_exist": true,
      "note": "The @inherits owner. Its name comes from the FILE PATH (componentNameFromPath never reads the file), which is why the hierarchy edge has to be anchored on this record rather than on a node the cross/hierarchy pass would mint." },
    { "name": "NavMenu", "kind": "SCOPE.UIComponent", "subtype": "component", "source_file": "Components/Layout/NavMenu.razor", "must_exist": true,
      "note": "The @implements owner — two directives, one component." },
    { "name": "Counter", "kind": "SCOPE.UIComponent", "subtype": "component", "source_file": "Components/Pages/Counter.razor", "must_exist": true,
      "note": "The negative control: no hierarchy directive, and three decoys that look like one. It must keep every entity and edge it had before #6370 while acquiring no hierarchy edge." },
    { "name": "IAppDisposable", "kind": "SCOPE.Component", "subtype": "interface", "source_file": "Shared/Abstractions.cs", "must_exist": true,
      "note": "The C# interface NavMenu implements. Present so the IMPLEMENTS edge below has a real node at the far end: without it the row would be graded on a dangling target, and per #6947 a dangling edge still de-orphans its source, so the orphan metric could never tell the difference." },
    { "name": "IAppTrackable", "kind": "SCOPE.Component", "subtype": "interface", "source_file": "Shared/Abstractions.cs", "must_exist": true,
      "note": "The second interface — the far end of the row that grades directive MULTIPLICITY." }
  ],
  "expected_relationships": [
    { "from_name": "MainLayout", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Layout/MainLayout.razor",
      "kind": "EXTENDS", "to_name": "AppLayoutBase", "to_kind": "SCOPE.UIComponent", "to_file": "Components/Layout/AppLayoutBase.razor",
      "must_exist": true,
      "note": "#6370: `@inherits AppLayoutBase`. The razor->razor case, and the only row that grades EXTENDS at all. FROM is the component in MainLayout.razor and TO is the component in a DIFFERENT file, so a producer that anchored the edge on the file entity, or that resolved the target within the owning file only, fails here." },
    { "from_name": "NavMenu", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Layout/NavMenu.razor",
      "kind": "IMPLEMENTS", "to_name": "IAppDisposable", "to_kind": "SCOPE.Component", "to_file": "Shared/Abstractions.cs",
      "must_exist": true,
      "note": "#6370: FIRST `@implements` directive, and the CROSS-LANGUAGE case — the interface is declared in C#, which is where a Blazor component's interfaces actually live. Held constant with the row below except for which directive line it came from." },
    { "from_name": "NavMenu", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Layout/NavMenu.razor",
      "kind": "IMPLEMENTS", "to_name": "IAppTrackable", "to_kind": "SCOPE.Component", "to_file": "Shared/Abstractions.cs",
      "must_exist": true,
      "note": "#6370: SECOND `@implements` directive on the SAME component. This is the only row that grades multiplicity — Razor spells multiple interfaces as repeated directive LINES, not as a comma list, so a producer that took the first match (FindStringSubmatch rather than FindAll) satisfies the row above and fails this one." },
    { "from_name": "NavMenu", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Layout/NavMenu.razor",
      "kind": "CONTAINS", "to_name": "Dispose", "to_kind": "SCOPE.Operation", "to_file": "Components/Layout/NavMenu.razor",
      "must_exist": true,
      "note": "Pre-existing razor behaviour on the component that also carries hierarchy edges: the @code block's event handler. Held so a regression that dropped ALL embedded relationships is distinguishable from one that dropped only the hierarchy kinds — both would otherwise read the same way." },
    { "from_name": "Counter", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Pages/Counter.razor",
      "kind": "CONTAINS", "to_name": "Increment", "to_kind": "SCOPE.Operation", "to_file": "Components/Pages/Counter.razor",
      "must_exist": true,
      "note": "The same pre-existing behaviour on the component with NO hierarchy directive: Counter must keep its member edge while acquiring no EXTENDS/IMPLEMENTS." }
  ],
  "forbidden_relationships": [
    { "from_name": "Counter", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Pages/Counter.razor",
      "kind": "EXTENDS", "to_bare_name": "WraithBase",
      "note": "#6370 OVER-FIRING, the line-leading anchor: Counter.razor's markup contains `<p>A directive is line-leading; @inherits WraithBase here is markup text.</p>`. A directive is only a directive at the start of its line. FIRES, verified: deleting the `^` from reInherits produces exactly Counter -> WraithBase and the gate reports one forbidden hit." },
    { "from_name": "Counter", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Pages/Counter.razor",
      "kind": "EXTENDS", "to_bare_name": "GhostBase",
      "note": "#6370 OVER-FIRING, one-line Razor comment: `@* @inherits GhostBase *@`. Note this decoy is NOT line-leading either, so the anchor alone rejects it and it does not grade the scrub on its own — it is the pair to the row below, which is the one that does. FIRES, verified: with BOTH the `^` anchor and scrubRazorComments removed, this row and WraithBase fire together." },
    { "from_name": "Counter", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Pages/Counter.razor",
      "kind": "IMPLEMENTS", "to_bare_name": "IPhantom",
      "note": "#6370 OVER-FIRING, MULTI-LINE Razor comment — the row that grades scrubRazorComments by itself. `@implements IPhantom` sits at column 0 on its own line INSIDE a `@*` … `*@` block, so it is line-leading and the anchor cannot reject it: the scrub is the only thing that does. FIRES, verified: disabling scrubRazorComments alone (returning src unchanged) produces exactly Counter -> IPhantom, with the anchor left intact." },
    { "from_name": "MainLayout", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Layout/MainLayout.razor",
      "kind": "IMPLEMENTS", "to_name": "AppLayoutBase", "to_kind": "SCOPE.UIComponent", "to_file": "Components/Layout/AppLayoutBase.razor",
      "note": "#6370 KIND EXCLUSIVITY, @inherits half. `@inherits` is single inheritance and is EXTENDS, never IMPLEMENTS. Emitting both kinds costs nothing in recall — the must_exist EXTENDS row still binds — so the exclusivity is ungraded in the permissive direction unless it is fenced here. FIRES, verified: changing add(\"EXTENDS\", reInherits) to add(\"IMPLEMENTS\", reInherits) fails the must_exist row AND registers this forbidden hit; adding a second add(\"IMPLEMENTS\", reInherits) beside the first keeps recall at 100% and is caught by this row ALONE." },
    { "from_name": "NavMenu", "from_kind": "SCOPE.UIComponent", "from_file": "Components/Layout/NavMenu.razor",
      "kind": "EXTENDS", "to_name": "IAppDisposable", "to_kind": "SCOPE.Component", "to_file": "Shared/Abstractions.cs",
      "note": "#6370 KIND EXCLUSIVITY, @implements half — the mirror of the row above, so the two directives are fenced independently rather than one standing in for both. FIRES, verified: an extra add(\"EXTENDS\", reImplements) leaves every must_exist row satisfied and is caught only here." }
  ],
  "forbidden_entities": [
    { "name": "AppLayoutBase", "kind": "SCOPE.Component", "source_file": "Components/Layout/MainLayout.razor",
      "note": "#6370: THE failure mode of the rejected implementation route. Registering razor in cross/hierarchy's supportedLanguages mints a SCOPE.Component for the type AND one for each parent, so the base named by MainLayout's directive would appear a second time, as a Component attributed to MainLayout.razor, with the edge anchored on it instead of on the real component. The genuine AppLayoutBase is a SCOPE.UIComponent in its OWN file (must_exist above), so the two rows cannot be satisfied by the same record. NOT FIRED by any mutant of this extractor — it cannot be, since this extractor mints entities only from file paths — and it is kept as a fence on the route rather than on the code, which is what the pony fixture's equivalent rows record about themselves." },
    { "name": "GhostBase", "kind": "SCOPE.UIComponent", "source_file": "Components/Pages/Counter.razor",
      "note": "#6370: the base named inside Counter.razor's one-line Razor comment. UNREACHABLE by any mutant of this extractor, stated plainly rather than implied: razor entity names come from componentNameFromPath, which never reads the file, so no loosening of the directive patterns can mint an entity named after a directive's ARGUMENT. Kept as a fence for a rule-pack producer, which is the shape that does exactly this (internal/engine/rules/graphql/frameworks/graphql_schema.yaml does it for commented-out SDL)." },
    { "name": "IPhantom", "kind": "SCOPE.Component", "source_file": "Components/Pages/Counter.razor",
      "note": "#6370: the interface named inside the multi-line Razor comment. Same grading status as the row above — an unreachable fence, not a mutant-killed row." }
  ]
}
