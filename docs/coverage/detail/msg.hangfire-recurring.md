<!-- DO NOT EDIT — generated from docs/coverage/registry.json by 'go run ./tools/coverage gen' -->
# `msg.hangfire-recurring` — Hangfire RecurringJob (.NET scheduled jobs)

Auto-generated. Back to [summary](../summary.md).

- **Language:** [C#](../by-language/csharp.md)
- **Category:** [message_broker](../by-category/message_broker.md)
- **Subcategory:** Schedulers
- **Capability cells:** 2

## Capabilities

| Capability | Status | Verified at | Issue | Cites | Notes |
|------------|--------|-------------|-------|-------|-------|
| Consumer extraction | 🟢 `partial` | — | 3628 | `internal/custom/csharp/hangfire.go`<br>`internal/engine/scheduled_jobs_edges.go`<br>`internal/engine/scheduled_jobs_edges_test.go` | #3628 area: RecurringJob.AddOrUpdate("id", () => Type.Method(), SCHEDULE) and the generic AddOrUpdate<T>("id", x => x.Method(), SCHEDULE) form emit a SCOPE.ScheduledJob (hangfire_recurring:<id>) carrying the schedule with a TRIGGERS edge to the handler. #4922: the custom_csharp_hangfire extractor ALSO recovers the consumer side -- class X : IJob/IBackgroundJob -> SCOPE.Service(job_class) and [AutomaticRetry] -> SCOPE.Pattern(retry_policy). #6741 arm 5 correction: this record used to say those two were "both CONSUMES", which was true only of an inert `edge_kind: CONSUMES` entity property (removed in arm 5). NO CONSUMES EDGE IS EMITTED -- not by this pass, and not by any pass in any language: the kind is declared in internal/types/kinds.go and read by four consumers, and nothing produces it (known gap, ADR-0028). The consumer is reached instead by the TRIGGERS edge scheduled_jobs_edges.go emits from the SCOPE.ScheduledJob, where its target resolves -- in the csharp-hangfire-mini golden it does not, because the receiver CleanupService is dropped from the lambda and only the bare name Run survives, so that row stays nice_to_have. Honest-partial: dynamic schedules not parsed. |
| Producer extraction | ✅ `full` | `2026-06-14` | 5085 | `internal/custom/csharp/extractors_test.go`<br>`internal/custom/csharp/hangfire.go`<br>`internal/custom/csharp/hangfire_schedule_test.go` | #4922: custom_csharp_hangfire emits the producer side -- BackgroundJob.Enqueue(() => T.M()) and the typed Enqueue<T>(x => x.M()) -> SCOPE.Operation(task_enqueue); BackgroundJob.Schedule(() => T.M(), delay) -> SCOPE.Operation(task_schedule); RecurringJob.AddOrUpdate -> SCOPE.Pattern(recurring_job). Each such entity carries a task_id: task:hangfire:<Type>.<Method> for the Enqueue and Schedule forms, task:hangfire:recurring:<id> for the RecurringJob form. #5015: now FULL on the producer axis -- (a) Hangfire Cron.* fluent helpers (Cron.Daily/Hourly/Weekly/Monthly/Yearly/Minutely) and raw 5/6-field cron string literals parse onto the recurring_job node as cron_expression + schedule_type (interval helpers like Cron.MinuteInterval(n) record schedule_type=interval, arg runtime-dependent); (b) dynamic / non-literal call-sites -- captured-variable job-ids, nested member-access lambda bodies (a.b.Method()), method-group enqueue -- no longer drop: they emit an honest unresolved producer (pattern_type recurring_dynamic / enqueue_dynamic, resolution=unresolved) so the dispatch site stays in-graph as an ENTITY -- it names no type, so it gets no PRODUCES edge (see #6741 below) -- with cron still parsed when present. The schedule-carrying ScheduledJob node + TRIGGERS edge remains the complementary scheduled_jobs_edges.go path. #5085: the deferred producer follow-ups land -- (a) argument-bearing Cron.* overloads with LITERAL args now resolve a real cron_expression: Cron.Daily(h)/Daily(h,m), Cron.Hourly(m), Cron.Weekly(DayOfWeek.X[,h[,m]]), Cron.Monthly(d[,h[,m]]), Cron.Yearly(mo[,d[,h[,m]]]), and the interval helpers Cron.MinuteInterval(n)/HourInterval/DayInterval/MonthInterval -> */n step expressions (non-literal args still fall back to schedule_type label only, never fabricated); (b) local-dataflow resolution of a captured-variable job-id -- a same-file var/string/const-string assignment is recovered so RecurringJob.AddOrUpdate(jobId, ...) becomes recurring:<id> with resolution=resolved_dataflow + task_id (unresolvable ids stay honestly unresolved); (c) method-group enqueue BackgroundJob.Enqueue<T>(x => x.Method) (no call) gets first-class typed resolution as pattern_type=enqueue_method_group rather than being swept into the dynamic fallback. #6741 arms 3+5 (2026-09-02) -- RE-DERIVED against what is emitted, not against the inert property this record used to describe. Until arm 3 this pass emitted ZERO relationships: the word PRODUCES here named an `edge_kind` entity PROPERTY, and no edge of that kind existed anywhere in C#. Arm 3 made it real. internal/custom/csharp/helpers.go csJobProducesEdge now emits a PRODUCES relationship from every dispatch entity whose job type resolved (subtypes task_enqueue / task_schedule / recurring_job with a non-empty job_type) to the resolvable stub Class:<Type>, carrying framework / job_class / provenance / task_id. Dispatch sites that resolve no type (enqueue_dynamic, recurring_dynamic) deliberately emit no edge, so "all producers carry a PRODUCES edge" would be false; what is true is that every RESOLVED dispatch site does. Arm 5 removed the inert `edge_kind` property from this pass. The producer axis stays full; the outstanding gap is the other half -- nothing emits CONSUMES, in any language (ADR-0028). |

## Provenance

This record is sourced from `docs/coverage/registry.json`. To update it, edit the JSON
(or use `go run ./tools/coverage update msg.hangfire-recurring ...`) then regenerate:

```
go run ./tools/coverage validate
go run ./tools/coverage gen
```
