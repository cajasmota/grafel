<!-- DO NOT EDIT — generated from docs/coverage/registry.json by 'go run ./tools/coverage gen' -->
# `lang.vbnet.core` — VB.NET

Auto-generated. Back to [summary](../summary.md).

- **Language:** [VB.NET](../by-language/vbnet.md)
- **Category:** [language](../by-category/language.md)
- **Capability cells:** 3

## Capabilities

| Capability | Status | Verified at | Issue | Cites | Notes |
|------------|--------|-------------|-------|-------|-------|
| Call line precision | ✅ `full` | `2026-08-19` | — | `internal/extractors/vbnet/extractor.go`<br>`internal/extractors/vbnet/extractor_6327_test.go`<br>`internal/vbnet/parenrefs.go`<br>`internal/vbnet/symbols.go` | #6327 S5: every CALLS edge carries a 1-based FILE-ABSOLUTE line Property taken from the use site itself (vbnet.Ref.Line), not from the enclosing member, so it is directly clickable to source; CONTAINS and IMPORTS are line-stamped too. Call-vs-not is decided ONLY by vbnet.Ref.IsCall(), which composes the per-file declaration table's ParenKind verdict with the New prefix and the statement-head rule — VB reuses parentheses for array indexing and default properties, so a 'looks like a call' regex is exactly the phantom-CALLS failure this epic exists to prevent; intrinsics (CType, CInt, GetType) are call-SHAPED but resolve to no declaration and are excluded, which matters because one WinForms designer file in the corpus contains hundreds of CType. callTarget independently refuses a Ref that is Qualified with an empty Qualifier (a With-block target or an expression result — real but unnameable per-file; 1,624 of 41,748 use sites measured) so the invariant does not rest on IsCall composition holding, strips Me/MyClass/MyBase (which denote the current instance and would guarantee a dangling edge), and calls are de-duplicated per owning declaration. MEASURED on the 302-file corpus: CALLS 5,059 total, 3,386 resolved (67%); the residue is BCL/WinForms members with no in-tree declaration. Honest caveat on the sibling edges: EXTENDS/IMPLEMENTS carry the TYPE's declaration line rather than the Inherits/Implements clause line, because vbnet.Node records those clauses as a []string with no positions. |
| Core extraction | 🟢 `partial` | `2026-08-20` | 6327 | `internal/extractors/vbnet/corpus_6327_test.go`<br>`internal/extractors/vbnet/extractor.go`<br>`internal/extractors/vbnet/extractor_6327_test.go`<br>`internal/extractors/vbnet/partial.go`<br>`internal/extractors/vbnet/references.go`<br>`internal/vbnet/parse.go`<br>`internal/vbnet/symbols.go` | #6327 S4 (#6381 parser) + S5 (extractor). internal/vbnet is a hand-written recursive-descent line scanner (no tree-sitter VB grammar exists) with a per-file declaration/symbol table; internal/extractors/vbnet projects its containment tree onto entities and the four edge kinds the epic names. Kind mapping follows the C# extractor so a mixed .NET repo indexes uniformly: Class/Module/Structure/Interface/Delegate to SCOPE.Component (subtype = the VB keyword), Enum and data members to SCOPE.Schema, callable members to SCOPE.Operation; Namespace is NOT an entity (stamped as the vbnet_namespace property, mirroring csharp.go) because a node per file would duplicate once per file sharing the namespace (92 files for StaxRip.UI alone). CONTAINS is emitted from the nearest entity-bearing ancestor, with the file carrier owning top-level declarations. EXTENDS/IMPLEMENTS are emitted EMBEDDED on the owning type with FromID EMPTY so assembly substitutes the owner id — a path-valued FromID would be rewritten by ReferencesEmbedded onto the file carrier and merge every type's bases onto one node (#6295/#6298/#6365/#6367). MEASURED on a 302-file real corpus (WakeOnLAN, StaxRip, display-drivers-uninstaller; 13,479 records) after the real Path-B resolution chain: CONTAINS 13,205/13,205 resolved, EXTENDS 153/289, IMPLEMENTS 15/72 — the unresolved hierarchy targets are .NET Framework base types, classified as external-known rather than excused in-tree (internal/resolve/refs.go isVBExternalBaseType + vbnet_basetypes_6327_test.go). S7 lifted three of the four limits S5 recorded. S7a (#6412) merges a partial class split across .vb/.Designer.vb before graph.EntityID is formed, since that id hashes sourceFile and two halves cannot be made to collide after the fact: 1,046 distinct SCOPE.Component ids before, 1,012 after, 34 duplicates removed; the residual 34 are same-named classes in DIFFERENT .vbproj projects that must stay distinct (MySettings x5, Resources x5) plus 3 types split across files with no Partial pairing, which need a compilation-unit model grafel does not have. S7b (#6426) emits Handles and AddressOf as REFERENCES stamped via=handles or addressof, 0 edges before and 1,212 after, 78.5% resolved. S7c (#6445) emits method/property/event-level Implements IFoo.Bar as IMPLEMENTS stamped via=implements-member, 0 before and 137 emitted / 32 resolved after, with type-level unchanged at 72/15 — member-level clauses are 2.1x more common than type-level in the corpus, 137 against 65. STILL PARTIAL, and the residue is decisions rather than gaps: hierarchy edges remain stamped with the type's declaration line, not the Inherits/Implements clause line, because vbnet.Node records those clauses as a []string with no positions; With-block member accesses emit no edge at all (S7d, closed unbuilt after a full end-to-end simulation — 50 blocks, 281 member sites, 112 of them calls, collapsing to 25 CALLS edges of which 1 resolves, so +0.5% edges and +0.03% resolved against 24 new dangling ones), and the recall behind them is local-variable type inference (#6454), not With-block bookkeeping; two overloads in one type still share an entity id because declName omits the parameter list (#6440, 4 collisions in the corpus); ASP.NET WebForms markup is not parsed (S7e, closed unbuilt — the double extension never broke classification, .aspx.vb code-behind is fully extracted and .aspx.designer.vb pairs with it through S7a, so what is absent is markup-attribute handler wiring, the C#-idiomatic form of what Handles already covers). |
| Import resolution quality | 🟢 `partial` | `2026-08-19` | 6327 | `internal/extractors/vbnet/corpus_6327_test.go`<br>`internal/extractors/vbnet/extractor.go`<br>`internal/extractors/vbnet/extractor_6327_test.go`<br>`internal/resolve/imports.go` | #6327 S5: Imports statements (plain and aliased) emit one IMPORTS edge per distinct (local_name, module) pair, hung on the per-file SCOPE.Component carrier with FromID = the importing file's path — the ONE place a path-valued FromID is correct, because BuildImportTable (internal/resolve/imports.go) keys the per-file binding on it. No per-import placeholder entity is emitted (the #742/#681/#693 pattern solidity moved to in #6371): a placeholder named after the import's leaf segment collides by graph.EntityID with a same-named declaration in the same file (#6368/#6369). Each edge carries imported_name/local_name/source_module/line, written in ascending key order because types.Props is a sorted slice binary-searched by Props.Get. PARTIAL, and the number is the point: MEASURED on the 302-file corpus only 1 of 763 IMPORTS edges resolves to an in-tree node. That is not a parsing defect — VB.NET files import .NET Framework namespaces (System.Windows.Forms, Microsoft.Win32, ...) that have no source in the indexed tree, so the correct disposition is external, not resolved. Project-internal namespace imports do resolve. Raising the ratio needs namespace-level target synthesis / assembly-reference awareness, deferred to S7. |

## Provenance

This record is sourced from `docs/coverage/registry.json`. To update it, edit the JSON
(or use `go run ./tools/coverage update lang.vbnet.core ...`) then regenerate:

```
go run ./tools/coverage validate
go run ./tools/coverage gen
```
