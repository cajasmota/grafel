import { DataType } from 'apache-arrow';
import { TimelineMode } from '@cosmograph/ui';
export type TimelineSelection = [Date, Date] | [number, number];
export declare const resolveTimelineDataMode: (mode: TimelineMode | undefined, data: (number | Date)[] | undefined) => TimelineMode;
export declare const toDates: (selection: TimelineSelection | undefined, mode: TimelineMode) => TimelineSelection | undefined;
export declare const toNumbers: (selection: TimelineSelection | undefined, mode: TimelineMode) => [number, number] | undefined;
/** Map timeline selection to values matching the data column type for range filtering. */
export declare const toRangeFilterSelection: (columnType: DataType | undefined, selection: TimelineSelection, mode: TimelineMode) => [Date, Date] | [number, number];
export declare const toUTCExtent: (customExtent: [number, number] | undefined, mode: TimelineMode) => [number, number] | undefined;
